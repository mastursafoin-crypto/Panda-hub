-- PANDA HUB - BLUE LOCK WITH FORCED DISCORD JOIN & FEATURES
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer

-- نظام التحقق من الدخول لسيرفر الديسكورد أولاً
local Verified = false

if CoreGui:FindFirstChild("PandaHubVerifyUI") then CoreGui.PandaHubVerifyUI:Destroy() end
if CoreGui:FindFirstChild("PandaHubUI") then CoreGui.PandaHubUI:Destroy() end

local VerifyGui = Instance.new("ScreenGui", CoreGui)
VerifyGui.Name = "PandaHubVerifyUI"

local VFrame = Instance.new("Frame", VerifyGui)
VFrame.Size = UDim2.new(0, 340, 0, 180)
VFrame.Position = UDim2.new(0.5, -170, 0.5, -90)
VFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
VFrame.Active = true
VFrame.Draggable = true

Instance.new("UICorner", VFrame).CornerRadius = UDim.new(0, 12)
local VStroke = Instance.new("UIStroke", VFrame)
VStroke.Thickness = 2
VStroke.Color = Color3.fromRGB(255, 0, 0)

local VTitle = Instance.new("TextLabel", VFrame)
VTitle.Size = UDim2.new(1, 0, 0, 40)
VTitle.Position = UDim2.new(0, 0, 0, 10)
VTitle.BackgroundTransparency = 1
VTitle.Text = "Panda Hub - Verification"
VTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
VTitle.Font = Enum.Font.GothamBold
VTitle.TextSize = 16

local VDesc = Instance.new("TextLabel", VFrame)
VDesc.Size = UDim2.new(1, -20, 0, 40)
VDesc.Position = UDim2.new(0, 10, 0, 50)
VDesc.BackgroundTransparency = 1
VDesc.Text = "يجب عليك الانضمام لسيرفر الديسكورد أولاً لتشغيل السكربت!"
VDesc.TextColor3 = Color3.fromRGB(200, 200, 200)
VDesc.Font = Enum.Font.Gotham
VDesc.TextSize = 13
VDesc.TextWrapped = true

local BtnJoin = Instance.new("TextButton", VFrame)
BtnJoin.Size = UDim2.new(0, 300, 0, 35)
BtnJoin.Position = UDim2.new(0.5, -150, 0, 95)
BtnJoin.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
BtnJoin.Text = "1. اضغط لنسخ رابط ديسكورد والانضمام"
BtnJoin.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnJoin.Font = Enum.Font.GothamBold
BtnJoin.TextSize = 12
Instance.new("UICorner", BtnJoin).CornerRadius = UDim.new(0, 8)

local BtnContinue = Instance.new("TextButton", VFrame)
BtnContinue.Size = UDim2.new(0, 300, 0, 35)
BtnContinue.Position = UDim2.new(0.5, -150, 0, 135)
BtnContinue.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
BtnContinue.Text = "2. دخلت السيرفر؟ اضغط هنا للتشغيل"
BtnContinue.TextColor3 = Color3.fromRGB(100, 255, 100)
BtnContinue.Font = Enum.Font.GothamBold
BtnContinue.TextSize = 12
Instance.new("UICorner", BtnContinue).CornerRadius = UDim.new(0, 8)

BtnJoin.MouseButton1Click:Connect(function()
    pcall(function()
        setclipboard("https://discord.gg/wb58RgSp")
    end)
    BtnJoin.Text = "تم نسخ الرابط! توجه للديسكورد وانضم"
end)

BtnContinue.MouseButton1Click:Connect(function()
    Verified = true
    VerifyGui:Destroy()
    StartMainScript()
end)

-- دالة تشغيل القائمة الرئيسية بعد التحقق
function StartMainScript()
    local Gui = Instance.new("ScreenGui", CoreGui)
    Gui.Name = "PandaHubUI"

    local Frame = Instance.new("Frame", Gui)
    Frame.Size = UDim2.new(0, 360, 0, 300)
    Frame.Position = UDim2.new(0.5, -180, 0.5, -150)
    Frame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    Frame.Active = true
    Frame.Draggable = true

    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 12)
    local Stroke = Instance.new("UIStroke", Frame)
    Stroke.Thickness = 2
    Stroke.Color = Color3.fromRGB(255, 0, 0)

    local Top = Instance.new("Frame", Frame)
    Top.Size = UDim2.new(1, 0, 0, 40)
    Top.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Instance.new("UICorner", Top).CornerRadius = UDim.new(0, 12)

    local Title = Instance.new("TextLabel", Top)
    Title.Size = UDim2.new(1, 0, 1, 0)
    Title.Position = UDim2.new(0, 15, 0, 0)
    Title.BackgroundTransparency = 1
    Title.Text = "Panda Hub - Blue Lock"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.Font = Enum.Font.GothamBold
    Title.TextSize = 15
    Title.TextXAlignment = Enum.TextXAlignment.Left

    local Close = Instance.new("TextButton", Top)
    Close.Size = UDim2.new(0, 40, 0, 40)
    Close.Position = UDim2.new(1, -40, 0, 0)
    Close.BackgroundTransparency = 1
    Close.Text = "X"
    Close.TextColor3 = Color3.fromRGB(200, 200, 200)
    Close.Font = Enum.Font.GothamBold
    Close.TextSize = 14
    Close.MouseButton1Click:Connect(function() Gui:Destroy() end)

    local function MakeBtn(posY, txt)
        local b = Instance.new("TextButton", Frame)
        b.Size = UDim2.new(0, 320, 0, 40)
        b.Position = UDim2.new(0.5, -160, 0, posY)
        b.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        b.Text = txt
        b.TextColor3 = Color3.fromRGB(255, 255, 255)
        b.Font = Enum.Font.GothamBold
        b.TextSize = 13
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
        return b
    end

    local BtnBall = MakeBtn(50, "Ball Magnet (جلب الكورة): OFF")
    local BtnSize = MakeBtn(100, "Big Size (تكبير الشخصية): OFF")
    local BtnGoal = MakeBtn(150, "Auto Goal (التسجيل التلقائي): OFF")
    local BtnDisc = MakeBtn(200, "Discord: اضغط لنسخ الرابط")

    local BallOn, SizeOn, GoalOn = false, false, false

    BtnBall.MouseButton1Click:Connect(function()
        BallOn = not BallOn
        BtnBall.Text = "Ball Magnet (جلب الكورة): " .. (BallOn and "ON" or "OFF")
        BtnBall.TextColor3 = BallOn and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 255, 255)
    end)

    BtnSize.MouseButton1Click:Connect(function()
        SizeOn = not SizeOn
        BtnSize.Text = "Big Size (تكبير الشخصية): " .. (SizeOn and "ON" or "OFF")
        BtnSize.TextColor3 = SizeOn and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 255, 255)
        
        if LP.Character and LP.Character:FindFirstChild("Humanoid") then
            local hum = LP.Character.Humanoid
            pcall(function()
                if SizeOn then
                    hum.BodyDepthScale = 2
                    hum.BodyHeightScale = 2
                    hum.BodyWidthScale = 2
                    hum.HeadScale = 1.8
                else
                    hum.BodyDepthScale = 1
                    hum.BodyHeightScale = 1
                    hum.BodyWidthScale = 1
                    hum.HeadScale = 1
                end
            end)
        end
    end)

    BtnGoal.MouseButton1Click:Connect(function()
        GoalOn = not GoalOn
        BtnGoal.Text = "Auto Goal (التسجيل التلقائي): " .. (GoalOn and "ON" or "OFF")
        BtnGoal.TextColor3 = GoalOn and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 255, 255)
    end)

    BtnDisc.MouseButton1Click:Connect(function()
        pcall(function()
            setclipboard("https://discord.gg/wb58RgSp")
        end)
        BtnDisc.Text = "تم نسخ رابط الديسكورد بنجاح!"
        task.wait(2)
        BtnDisc.Text = "Discord: اضغط لنسخ الرابط"
    end)

    task.spawn(function()
        while Gui.Parent do
            Stroke.Color = Color3.fromHSV(tick()%5/5, 1, 1)
            task.wait(0.03)
        end
    end)

    local function FindTheBall()
        for _, v in ipairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") and not v.Anchored then
                local isPlayer = false
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Character and v:IsDescendantOf(p.Character) then
                        isPlayer = true
                        break
                    end
                end
                if not isPlayer then
                    local name = v.Name:lower()
                    if name:find("ball") or name:find("football") or name:find("soccer") or v.Shape == Enum.PartType.Ball then
                        return v
                    end
                end
            end
        end
        return nil
    end

    RunService.RenderStepped:Connect(function()
        if LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") then
            local root = LP.Character.HumanoidRootPart
            local ball = FindTheBall()
            
            if ball then
                if BallOn then
                    pcall(function()
                        ball.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        ball.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                        ball.CFrame = root.CFrame + (root.CFrame.LookVector * 2.5) + Vector3.new(0, -1, 0)
                    end)
                elseif GoalOn then
                    pcall(function()
                        for _, g in ipairs(workspace:GetDescendants()) do
                            if g.Name:lower():find("goal") or g.Name:lower():find("net") then
                                if g:IsA("BasePart") then
                                    ball.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                                    ball.CFrame = g.CFrame
                                    break
                                end
                            end
                        end
                    end)
                end
            end
        end
    end)
end
