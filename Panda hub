-- Panda Hub - Maintenance Mode
local CoreGui = game:GetService("CoreGui")

local DiscordInvite = "wb58RgSp"
local DiscordLink = "https://discord.gg/" .. DiscordInvite

-- مسح أي واجهة سابقة للسكربت
if CoreGui:FindFirstChild("PandaHub") then CoreGui.PandaHub:Destroy() end
if CoreGui:FindFirstChild("PandaLock") then CoreGui.PandaLock:Destroy() end
if CoreGui:FindFirstChild("PandaMaintenance") then CoreGui.PandaMaintenance:Destroy() end

-- إنشاء شاشة الصيانة
local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "PandaMaintenance"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 350, 0, 200)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -100)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 12)

local UIStroke = Instance.new("UIStroke", MainFrame)
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(255, 50, 50) -- إطار أحمر يدل على التوقف

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.Text = "Panda Hub 🐼 - وضع الصيانة"
Title.TextColor3 = Color3.fromRGB(255, 100, 100)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 20

local Message = Instance.new("TextLabel", MainFrame)
Message.Size = UDim2.new(0.9, 0, 0.5, 0)
Message.Position = UDim2.new(0.05, 0, 0.2, 0)
Message.BackgroundTransparency = 1
-- رسالتك اللي طلبتها بالحرف
Message.Text = "هناك صيانات من خبير سكربتات panda hub سيتم ألإنتهاء في أقرب وقت ولا تنسو تدخلون سيرفرنا ديسكورد"
Message.TextColor3 = Color3.fromRGB(255, 255, 255)
Message.Font = Enum.Font.SourceSans
Message.TextSize = 18
Message.TextWrapped = true

local DiscordBtn = Instance.new("TextButton", MainFrame)
DiscordBtn.Size = UDim2.new(0.8, 0, 0, 40)
DiscordBtn.Position = UDim2.new(0.1, 0, 0.75, 0)
DiscordBtn.BackgroundColor3 = Color3.fromRGB(114, 137, 218) -- لون الديسكورد
DiscordBtn.Text = "نسخ رابط الديسكورد"
DiscordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordBtn.Font = Enum.Font.SourceSansBold
DiscordBtn.TextSize = 16

local BtnCorner = Instance.new("UICorner", DiscordBtn)
BtnCorner.CornerRadius = UDim.new(0, 8)

-- دالة نسخ الرابط
DiscordBtn.MouseButton1Click:Connect(function()
    pcall(function() setclipboard(DiscordLink) end)
    DiscordBtn.Text = "تم النسخ! ننتظرك بالسيرفر"
    task.wait(2)
    DiscordBtn.Text = "نسخ رابط الديسكورد"
end)

